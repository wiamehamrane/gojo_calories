{
  "name": "gojocalories-functions",
  "description": "Cloud Functions for GojoCalories — OTP verification email delivery",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^5.0.0",
    "nodemailer": "^6.9.14"
  }
}
