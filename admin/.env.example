# ─────────────────────────────────────────────────────────────
# Admin panel (Next.js) — copy to `admin/.env.local`:
#   cp admin/.env.example admin/.env.local
# Never commit real env files with secrets.
# ─────────────────────────────────────────────────────────────

# Backend API base (include /api, no trailing slash preferred)
# Local backend:
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
NEXT_PUBLIC_API_URL=https://api.gojocalories.com/api
